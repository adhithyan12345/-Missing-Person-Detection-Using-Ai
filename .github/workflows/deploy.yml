name: Deploy to Hugging Face Spaces

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Push to hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git remote add space https://huggingface.co/spaces/adhithyan12345/missing-person-detection
          git push --force https://x-access-token:$HF_TOKEN@huggingface.co/spaces/adhithyan12345/missing-person-detection main
